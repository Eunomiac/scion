<form class="{{cssClass}} {{actor.type}} {{data.pantheon}}" autocomplete="off">

    {{!-- Radial Menu Tab Navigation --}}
    <nav class="sheet-tabs menuRosette" data-group="primary">
        <div class="circleMenuContainer">
          <div class="menuButtonContainer">
            <div class="menuButton">
              <div class="menuIcon iconSettings">
                <a class="item" data-tab="details"></a>
              </div>
              <div class="menuText">{{loc "scion.heading.main.detailsTab"}}</div>
            </div>
          </div>
          <div class="menuButtonContainer">
            <div class="menuButton">
              <div class="menuIcon iconMain">
                  <a class="item" data-tab="front"></a>
              </div>
              <div class="menuText">{{loc "scion.heading.main.frontTab"}}</div>
            </div>
          </div>
          <div class="menuButtonContainer">
            <div class="menuButton">
              <div class="menuIcon iconHeritage">
                <a class="item" data-tab="heritage"></a>
              </div>
              <div class="menuText">{{loc "scion.heading.main.heritageTab"}}</div>
            </div>
          </div>
          <div class="menuButtonContainer">
            <div class="menuButton">
              <div class="menuIcon iconExperience">
                <a class="item" data-tab="experience"></a>
              </div>
              <div class="menuText">{{loc "scion.heading.main.experienceTab"}}</div>
            </div>
          </div>
          <div class="menuButtonContainer">
            <div class="menuButton">
              <div class="menuIcon iconChargen">
                <a class="item" data-tab="chargen"></a>
              </div>
              <div class="menuText">{{loc "scion.heading.main.chargenTab"}}</div>
            </div>
          </div>
        </div>
    </nav>
    <div class="closeButton">❌</div>

    <section id="characterSheet" class="editable major {{data.pantheon}} grid main">
        {{!-- Sheet Header --}}
        <section class="header grid">
            <div class="divineTitle h2 noBG alignCenter">{{#if data.divineTitle}}&quot;{{~data.divineTitle~}}&quot;{{/if}}</div>
            <div class="charName title contentEditable alignCenter" data-field="name"></div>
            <div class="patronage h3 noBG alignCenter">&nbsp;{{~data.patronageLine~}}&nbsp;</div>
        </section>
            
        <section class="sheet-body grid">
            {{!-- Chargen Tab --}}
            <section class="tab flex-col" data-group="primary" data-tab="chargen">
                {{> (blocks.chargen.template) }}
            </section>
            {{!-- Front Tab --}}
            <section class="tab" data-group="primary" data-tab="front">
                <section class="front-body grid">
                    <div class="skills flex-col alignLeft">
                        <h1>{{loc "scion.game.skills"}}</h1>
                        {{#each skillVals as |value skill|}}
                        {{#if value}}
                        <div class="flex-col-start width100">
                        <div class="dotline">
                            <div class="label">{{loc (concat "scion.skill." skill)}}</div>
                            <div class="dotContainer">
                                {{#for value}}
                                    <span class="dot"></span>
                                {{/for}}
                            </div>
                        </div>
                        {{#if (test (concat (lookup ../skillSpecialties skill) ", ") "!=" "")}}
                        <div class="specSummary weak">{{concat (lookup ../skillSpecialties skill) ", "}}</div>
                        {{/if}}
                        </div>
                        {{/if}}
                        {{/each}}
                    </div>
                    <div class="paths flex-col alignLeft">
                      <h1>{{loc "scion.game.paths"}}</h1>
                    </div>
                    <div class="callings flex-col alignLeft">
                      <h1>{{loc "scion.game.callings"}}</h1>
                    </div>
                    <div class="deeds flex-col alignLeft">
                      <h1>{{loc "scion.game.deeds"}}</h1>
                    </div>
                    <div class="attributes flex-col-stretch alignCenter">
                      <h1>{{loc "scion.game.attributes"}}</h1>
                      <div class="attributeArenas flex-row-spaceBetween padded width100">
                      {{#each arenaPriorities as |arena|}}
                        <div class="arena-block flex-col-spaceEvenly width100">
                          {{#with (lookup ../arenas arena) as |attributes|}}
                          {{#each attributes as |value attribute|}}  
                          <div class="dotline">
                              <div class="label">{{loc (concat "scion.attribute." attribute)}}</div>
                              <div class="dotContainer">
                              {{#for value}}
                                  <span class="dot"></span>
                              {{/for}}
                              </div>
                          </div>
                          {{/each}}
                          {{/with}}
                        </div>
                      {{/each}}
                      </div>
                    </div>
                    <div class="virtues flex-row-center">
                      <div class="flex-row-spaceBetween-centercross padded height30">
                        <label class="alignLeft">{{virtues.[0]}}</label>
                        <div class="dotline noLabel">
                            <div class="dotContainer blanks">{{#for 5}}<span class="dot"></span>{{/for}}</div>
                            <div class="dotContainer">
                                {{#for 3}}
                                    <span class="dot"></span>
                                {{/for}}
                            </div>
                        </div>
                        <label class="alignRight">{{virtues.[1]}}</label>
                      </div>
                    </div>
                </section>
            </section>
            {{!-- Divine Genesis Tab --}}
            <section class="tab" data-group="primary" data-tab="genesis">
                <div class="genesis grid">
                    <div class="genesis text">Genesis</div>
                    <div class="paths text">Paths</div>
                    <div class="virtues text">Virtues</div>
                </div>
            </section>
            {{!-- Details Tab --}}
            <section class="tab" data-group="primary" data-tab="details">
                <div class="details grid">
                    <div class="attributes text">Attributes</div>
                    <div class="paths text">Paths</div>
                    <div class="virtues text">Virtues</div>
                </div>
            </section>
            {{!-- Experience Tab --}}
            <section class="tab" data-group="primary" data-tab="experience">
                <div class="experience grid">
                    <div class="attributes text">Attributes</div>
                    <div class="paths text">Paths</div>
                    <div class="virtues text">Virtues</div>
                </div>
            </section>
        </section>
    </section>
</form>